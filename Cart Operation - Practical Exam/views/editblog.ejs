<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Blog</title>
  
  <!-- Include Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Poppins', sans-serif;
      color: #333;
      margin: 0;
    }

    .container {
      max-width: 800px;
      padding: 20px;
    }

    .card {
      background: #ffffff;
      border: none;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .card-header {
      background: #fff;
      color: #000;
      border: 4px solid #000;
      border-radius: 15px 15px 0 0;
      text-align: center;
      font-size: 1.8rem;
      padding: 15px;
      font-weight: 600;
    }

    .form-label {
      font-weight: 500;
      color: #444;
      margin-bottom: 5px;
    }

    .form-control {
      border-radius: 10px;
      border: 1px solid #ddd;
      padding: 10px;
      transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }

    .form-control:focus {
      border-color: #6c63ff;
      box-shadow: 0 0 8px rgba(108, 99, 255, 0.5);
    }

    .btn-primary {
      background-color: #ffffff;
      color: #030303;
      border: 1px solid #000;
      border-radius: 10px;
      font-size: 1.1rem;
      font-weight: 600;
      padding: 10px 20px;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .btn-primary:hover {
      background-color: #000;
      transform: scale(1.05);
    }

    img#image {
      display: block;
      margin: 15px auto;
      object-fit: cover;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
    }

    small {
      display: block;
      margin-top: 10px;
      color: #777;
    }

    @media (max-width: 768px) {
      .card-header {
        font-size: 1.5rem;
      }

      .btn-primary {
        font-size: 1rem;
        padding: 8px 16px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="card">
      <div class="card-header">
        Edit Your Blog
      </div>
      <div class="card-body">
        <form action="/updateblog" method="POST" enctype="multipart/form-data">
          <input type="hidden" name="id" value="<%= blog._id %>">

          <!-- Image Upload -->
          <div class="mb-3">
            <label for="image" class="form-label" style="font-weight: 800;">Upload Image</label>
            <input type="file" class="form-control" id="image" name="image" accept="image/*" />
            <img src="<%= blog.image %>" width="350" height="200" alt="Current Blog Image" id="image" />
            <input type="hidden" value="<%= blog.image %>" name="existingImage">
            <small>Your current file is shown above. If you want to change it, select a new one from "Choose File."</small>
          </div>

          <!-- Title -->
          <div class="mb-3">
            <label for="title" class="form-label" style="font-weight: 800;">Title</label>
            <input type="text" class="form-control" value="<%= blog.title %>" id="title" name="title" placeholder="Enter title" required />
          </div>

          <!-- Caption -->
          <div class="mb-3">
            <label for="caption" class="form-label" style="font-weight: 800;">Caption</label>
            <textarea class="form-control" id="caption" name="caption" rows="3" placeholder="Write a caption..." required><%= blog.caption %></textarea>
          </div>

  
          <!-- Submit Button -->
          <div class="d-grid gap-2">
            <button type="submit" class="btn btn-primary">Update Blog</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
